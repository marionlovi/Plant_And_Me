
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container">
                <nav class="navbar" style="width: 100%; display: flex; justify-content: space-between; align-items: center;">
                    <a href="{{ path('app_home') }}" style="text-decoration: none; color: #333; font-size: 1.5em; font-weight: bold;">Mon Application</a>
                    
                    <ul class="navbar-nav" style="display: flex; list-style: none; margin: 0; padding: 0;">
                        <li class="nav-item" style="margin: 0 10px;">
                            <a href="{{ path('app_home') }}" style="text-decoration: none; color: #333; transition: color 0.3s ease;">Accueil</a>
                        </li>
                        <li class="nav-item" style="margin: 0 10px;">
                            <a href="{{ path('app_plante_index') }}" style="text-decoration: none; color: #333; transition: color 0.3s ease;">Plantes</a>
                        </li>
                        <li class="nav-item" style="margin: 0 10px;">
                            <a href="{{ path('app_soin_index') }}" style="text-decoration: none; color: #333; transition: color 0.3s ease;">Soins</a>
                        </li>
                        <li class="logout" style="margin: 0 10px;">
                            <a href="{{ path('app_logout') }}" style="text-decoration: none; font-size: 16px; color: #dc3545;">Logout</a>
                        </li>
                    </ul>
                </nav>
            </div>
        </nav>
                </nav>
            </div>
        </nav>
        {% block body %}{% endblock %}
    </body>
</html>
